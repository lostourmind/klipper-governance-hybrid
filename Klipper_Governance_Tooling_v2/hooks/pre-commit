#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
CFG="$ROOT/.klipperlint.toml"
python3 "$ROOT/tools/klipper_macro_lint.py" --paths "printer.cfg" "macros/**" --config "$CFG"
